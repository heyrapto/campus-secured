<section id="backups" class="aero-container py-12">
  <div class="aero-panel p-6 md:p-8">
    <p class="text-sm font-bold text-[var(--aero-accent)]">BACKUPS</p>
    <div class="mt-2 grid gap-6 md:grid-cols-[0.8fr_1.2fr]">
      <h2 class="text-4xl font-black">Local snapshots with optional R2 upload.</h2>
      <p class="leading-8 text-[var(--aero-muted)]">
        Store R2 credentials in Aeroplane for database backups, keep local disk snapshots, and upload archives
        to Cloudflare R2 when you want off-server recovery.
      </p>
    </div>
  </div>
</section>
